import { StyleSheet } from "react-native";
import { spacing } from "../../designSystem/theme/spacing";

const troottStyles = StyleSheet.create({
  authContainer: {
    marginHorizontal: spacing.space16,
  },
});

export default troottStyles;
